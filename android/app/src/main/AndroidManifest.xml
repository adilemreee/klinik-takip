<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
      The emergency button sends a location when it has one, and sends without
      one when it does not (spec M8). Coarse only: a clinic needs to know which
      hotel, not which room, and the finer permission is harder to justify to a
      patient and to a reviewer.
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
      No android:icon yet: the launcher icon has to be the clinic's own mark,
      and that is one of the items still outstanding in
      docs/KLINIKTEN-ISTENENLER.md. Shipping a placeholder is worse than
      shipping none.
    -->
    <application
        android:name=".KlinikApplication"
        android:label="@string/app_name"
        android:supportsRtl="false"
        android:theme="@style/Theme.Klinik"
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode|locale|layoutDirection"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
