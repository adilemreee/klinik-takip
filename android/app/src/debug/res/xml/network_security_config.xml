<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug only. Overrides the release configuration so a development build can
  reach a backend running on the developer's machine or through an SSH tunnel,
  which is plain HTTP over loopback. Nothing else is allowed to be cleartext,
  and this file is not in the release build at all.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
